# Core dependencies for the **web demo runtime**
# This file is intentionally minimal; heavy ML libs are moved to
# `requirements-full.txt` or downloaded dynamically.  Use this for
# Heroku slugs / small containers.

# ── Web framework ──
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
aiohttp>=3.10.0
websockets>=16.0

# ── TTS ──
edge-tts>=6.1.0
soundfile>=0.12.0

# ── Utilities ──
python-dotenv>=1.0.0
pydantic>=2.0.0

tqdm>=4.60.0
PyYAML>=6.0.0

# Optional extras (used only if you run models locally):
#   pip install -r requirements-full.txt
